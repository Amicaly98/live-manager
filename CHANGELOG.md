# 更新日志

## 1.1.0（2026-09-14，候选版本，待监督复核）

目标：桌面版稳定性移植（来自 bilibili-live-server 26c170c 的稳定性工作，
按桌面语义适配），版本基线 48e52f1。

### 新增

- 控制操作签发票据协议：前端每次新的控制意图（开播/恢复/执行任务/停止/
  换区/确认验证）先取一张服务端签发票据（`X-Operation-Token`），后端以
  控制代际校验；过期/重放返回 409，前端明确报错且不换票重发。
- 控制代际（control epoch）：停止推进代际并取消在途开播；停止前挂起的
  开播在平台返回后作废并撤销平台侧房间；旧停止重放只确认既有结果，
  绝不停掉后来开启的新直播。
- 用户停止意图持久化（`stop_intent.json`）：停止并退出后重开应用仅提示
  继续、不自动开播（桌面默认语义保持不变）；成功开播自动清除。
- 统一数据目录：所有可写文件（状态/数据库/Excel/cookies/日志/缓存）落在
  Electron userData 下的 `data/`；旧版散落数据首次启动时带备份迁移
  （绝不覆盖新数据）；打包版内置分区表种子，首启复制不覆盖。
- 打包后端：PyInstaller 单文件 `run.exe`（内置 Python 运行时），用户机器
  无需安装 Python。

### 修复

- 开播/停止竞态：慢开播（选任务/等跨日重置/平台慢返回）期间执行停止，
  旧版可能恢复直播或占用状态；新版停止优先并在每个可等待点复核代际。
- 网络重试：面板查询改为有界重试（3 次），不再无限占用；后台开播线程
  无限重试但受停止事件控制；断网不再卡死前端/后端。
- 状态查询失败（网络异常）不再触发重新开播——只记为未知并节流提示，
  不掐掉仍在推进的本地流；确认被平台关闭（查询成功且 live_status=0）
  才走限次重连。
- 重连恢复按实际推流方式判断：手动分区 + FFmpeg 设置同样恢复推流
  （旧版被"手动模式"挡住）；OBS 外部推流不创建/不杀掉用户进程。
- 删除全局 `taskkill /F /IM ffmpeg.exe`：仅回收本应用创建的进程树
  （PID 精确 + 进程树）；外部 FFmpeg/OBS/未知端口服务完全不受影响；
  推流进程回收失败时保留所有权并阻止重复创建。
- FFmpeg 参数：`-flvflags no_duration_filesize` 移到输出 URL 之前
  （copy/reencode 两模式），消除 trailing-option 告警与 FLV 头不更新；
  不引入 rtmp_buffer/genpts。
- Electron 端口处理：不再为腾出 8000 杀未知进程（旧版按 netstat PID
  直接 taskkill）；仅回收确认属于本应用上一代后端（PID 文件匹配）的
  监听者，未知占用明确报错并阻断启动。
- 后端生命周期状态机：重启/退出路径等待旧进程确认回收后才拉起新代；
  close 回调按代际核对；退出期间不自动复活后端；优雅关闭（HTTP
  shutdown → uvicorn should_exit）优先，强杀进程树仅兜底。
- 托盘退出注册从 Dashboard.vue 移至 App.vue：任务页/设置页同样可以退出。
- 前端网络重试经同一 axios 实例递归（旧版走全局 axios.request 绕过
  拦截器）；取消不当成网络异常重新发起；开播请求支持 AbortSignal，
  停止会取消在途开播。
- 日志容量控制：backend.log 轮转（5MB×3），ffmpeg.log 超限轮转；
  日志/数据/内置资源分离。
- 人脸验证的邮件/远程确认绑定控制代际：用户停止后迟到的确认不会
  重试开播。

### 安全

- 测试与诊断全流程使用合成数据目录与本地替身，不读取/修改真实账号
  状态；产物构建不含任何 cookies/令牌/数据库/日志（见执行报告核对项）。

### 已知限制

- macOS 产物未测试（本版不宣称 macOS 支持）。
- 服务器端"长会话异常退出的 5 秒退避"重连风暴策略按清单要求原样保留，
  未宣称修复。
- 诊断（进度 PIPE/媒体窗口/脱敏导出）整体延期，未留下半套管道改造。
