# 桌面 1.1.0 共同可靠性同步

来源：服务器 `7f7858eddec0f1799b011996e6537eddcf12e12a`。桌面起点：`9a7b7022624f8e38d50d7150af2584d1c9567260`。本次不修改服务器，不同步 Linux/systemd/OSS/主机诊断，也不增加桌面面板密码。

## 同步范围与桌面差异

| 范围 | 本次行为 | 桌面保留项 |
| --- | --- | --- |
| 任务与数据 | 事务内变更、结算、版本推进与快照；旧覆盖拒绝；导入收据和空覆盖保护；完成后重算排序 | 原任务公式、现有进度、Excel 工作流 |
| 会话与计时 | 当前任务身份复核；固定目标；平台新鲜观察确认有效时长，未知状态暂停新增确认；迟到响应隔离 | 重新打开只提示继续，不自动开播；OBS 不启动本地 FFmpeg |
| 认证与读取 | QR/退出意图代际；过期响应不覆盖新账号；任务读取消、单飞和写后刷新 | file://、hash 路由、本机 API、真实 Electron 桥接 |
| 界面 | 有界首屏启动与重试、单调时钟进度、即时操作事件、设置版本冲突与编辑保护 | 原托盘、文件选择、桌面窗口行为 |
| 通知 | 结构化事件、开关、固定投递 worker、有界合并、同源日报、身份绑定确认 | 确认链接在运行应用的同一电脑打开；面板也可确认，不隐含手机公网可达 |
| 生命周期 | 第二实例提前退出；退出在途/失败保留所有权、允许重试；未知端口不强杀 | userData、本地 run.exe、精确 Windows 进程树回收 |

`stability-contracts` 原镜像与来源锚点不冒充新同步的全部验证。原 20 项契约继续用于其声明的控制边界；新增业务由仓库测试、独立监督与产物验收另行验证。

## 数据升级与回退

- 用户数据保留在 Electron userData/data；升级、覆盖安装和种子复制不得覆盖已有用户分区缓存。
- 分区表不是 Git 资产。构建必须显式提供仓库外合法种子，记录其哈希、来源和包内固定文件名；种子日期不等于实时平台数据。
- 旧状态已累计时长作为迁移基线保留，不追溯扣除未知历史断流。恢复目标不能重新抽样；身份、日期或目标时长无法确认时明确拒绝恢复。
- 新数据库附加元数据/兼容列随任务事务提交。回退保留最新数据，不执行 DROP COLUMN，不用历史备份覆盖后续任务进度；旧版不具备新版的覆盖保护和有效计时语义。
- 1.0.0 的实际数据位置与启动工作目录有关，不能仅凭安装目录猜测。先确认源路径与目标缺失条件，再使用带备份迁移。

## 验证边界

发布候选需完成：离线 Python/前端/Electron 回归、独立失败反例复验、真实 FFmpeg 恢复与有界持续推进、新包 GUI/导入导出/托盘/重复打开/退出、安装卸载与数据保留。结果以最终交付清单和源码哈希为准，本文不单独宣布产物验收通过。

所有平台登录/直播响应和通知投递均在本地合成边界验证；不宣称真实账号、B 站活动有效天数、SMTP/Server酱收件箱、公网质量、macOS 或代码签名已验收。Windows 安装包未签名时可能出现系统信誉提示。

持续推流测试只能证明给定采样窗口内未检出缺口，不能证明任意时刻无中断。公网断流根因仍需服务器现场证据。
